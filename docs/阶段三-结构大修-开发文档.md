# 阶段三 · 结构大修 开发文档

> 版本：v3（2026-06-12 · 新建流程重构 + 主笔记锚点；建立流程已上 TestFlight build 1-5）
> 适用：BrainStrom iOS（SwiftUI · 繁中 · 工业美学 · 零第三方依赖 · 已上 TestFlight）
> 阅读对象：以国中生白话为主，技术细节为辅，工程师可直接照着做。
> v2 新增：第 12 节「开工前补洞」——4 个代理实地勘查后端现况 / update_spec 前后端契约 / NoteScreen 拆分摩擦 / 教练多笔记切片，把开工前的 4 个不确定点钉死。实作时第 2/3/4 刀请配合第 12 节读。

---

## 1. 一句话与设计哲学

**一句话：** 一个专案不再只是「一篇笔记」，而是一个有「身份」、能「自由记录」、还有「专属 AI 教练」的完整工作台。

**设计哲学（三句话记住）：**

1. **「一个专案 = 一张身份证 + 一叠笔记 + 一位教练」。** 身份证是这个专案的硬规格（用什么技术、放哪台服务器），笔记是你随手乱写的草稿，教练是看得到全貌、帮你想清楚的 AI。
2. **「该乱的地方让你尽情乱，该整齐的地方绝对整齐」。** 开发笔记可以随便写、贴连结、塞模组，怎么乱都行；但系统结构（身份证）必须干净、准确、可信任。
3. **「AI 是唯一的守门员，但永远只建议、不强迫」。** 任何东西要进系统结构，都得先过 AI 这一关（无后门）；但 AI 只会「建议」，最后一定是你点确认才算数。

这一阶段不是加功能，是**把房子的格局重新打通**：从「单层一个笔记页」升级成「专案首页（三分页）→ 多笔记内页」的四层导航树。

---

## 2. 三分页架构：AI 教练 / 开发笔记 / 系统结构

进入任何一个专案，顶部是三个分页（沿用既有 `segButton` 工业风格选项卡）：

| 分页 | 图示 | 白话职责 | 谁能写 |
| --- | --- | --- | --- |
| **AI 教练** | 🤖 | 看得到「整个专案」（身份证＋所有笔记），陪你发想、帮你想清楚 | AI 对话，人提问 |
| **开发笔记** | 📝 | 你的草稿本：随便写、贴 YT/GitHub 连结、加模组、请 AI 整理 | 人写为主，AI 协助 |
| **系统结构** | 🪪 | 专案的「身份证」：硬规格（名称 / 技术栈 / 资料库 / 上线方式） | **只有 AI 能写，人只能看** |

### 各自职责讲清楚

**🪪 系统结构（身份证）**
- 字段（硬需求）：**名称 / 技术栈（前端 · 后端 · API）/ 资料库 / 上线（服务器 · 部署方法）**，选填可扩充。
- **铁律：人只能看，不能直接改。** 要改？去跟 AI 讲，AI 给建议，你点确认。
- 空字段显示淡色占位：「— 尚未填写，编辑或让 AI 记录」。

**📝 开发笔记（智能笔记）**
- 一个专案可以有**很多篇**笔记（这一阶段才正式打开多笔记）。
- 预设「全看」：AI 默认会读整篇笔记。
- 少数内容可加 🔒 排除标记，AI 就看不到那一块（隐私 / 不想被 AI 干扰的草稿）。
- 可以：写字、贴连结（YT / GitHub）、加模组、按钮请 AI「记进结构」。
- 沿用既有「文章 / 卡片」双视图、Markdown 渲染、⚡点子助攻、版本链 undo/redo。

**🤖 AI 教练**
- 每个专案**一位**教练，看得到全貌（身份证＋所有笔记切片）。
- 两种玩法（第 6 节详述）：**引导发想（扩散）** 和 **结构厘清（收敛）**。
- 教练**绝不直接改任何东西**，只透过 proposal 建议钮给你选。

---

## 3. AI 守门员铁律：任何内容进结构，一律过 AI

**这是阶段三最重要的一条规矩，写死、不许有例外。**

> 任何东西（一个模组、一个随手灵感、一条贴上来的连结、任何内容）想进入「系统结构（身份证）」，
> **一律先经过 AI**（没有后门、没有手动直填的偷吃步），
> AI 产生建议 → **使用者点确认** → 才真正写入身份证。

### 资讯流图（文字版）

```
[你做的任何动作]
  · 在笔记里写「前端用 React」
  · 贴一条 Fly.io 部署连结
  · 拖一个模组进来
  · 在 AI 教练里聊到「资料库用 Postgres」
        │
        ▼
┌─────────────────────────────┐
│   AI（唯一守门员，无后门）      │  ← 后端 prompt 侦测「硬需求」
│   侦测到这是身份证该记的东西？    │
└─────────────────────────────┘
        │ 是
        ▼
   AI 吐出 proposal 建议钮
   「📇 记入结构：前端 = React」
        │
        ▼
   ┌──────────────┐
   │  使用者点确认？  │
   └──────────────┘
     │是          │否
     ▼            ▼
  写入身份证     什么都不发生
  (update_spec)  (结构保持原样)
```

**重点白话：**
- 系统结构页**没有「+ 新增」「直接编辑」按钮直通写入**。所有写入路径都收敛到 `update_spec` 这条「过 AI → 点确认」的通道。
- AI 只是**建议者**，不是决定者。你不点，永远不会变。
- 这样保证身份证永远「干净、可信、有人把关」，不会被随手乱填污染。

---

## 4. 资料模型变更

阶段三动三件事：**打开多笔记**、**新增身份证 SystemSpec**、**笔记智能排除 🔒**。底层其实大多已备妥，改动集中、可控。

### 4.1 打开多笔记（底层早就支援，只差 UI 入口）

- 现状：`SystemEntity.notes: [NoteEntity]` 一对多**早已存在**，`NoteEntity` 含 `systemID` 外键、级联删除（`deleteRule: .cascade`）。
- 但目前 UI 只用 `documentNote()` 惰性取「最新单笔」，等于假装只有一篇。
- **改动：** 不动资料模型，新增 `notes(in: systemID)` 查询（仓储已可提供），UI 层新增「笔记清单」入口。`NoteDocument` 初始化要支援传入 `noteID`，不再假设系统内笔记唯一。

### 4.2 新增系统身份证 SystemSpec

**领域模型（DomainModels.swift）新增 struct：**

`SystemSpec`：`name?` / `frontend?` / `backend?` / `apis: [String]?` / `database?` / `server?` / `deployMethod?` —— 全部 optional String，选填、可扩充。

**持久化（二选一，建议方案 A 内嵌 JSON，最省事、仿既有 `tagsData`）：**
- **方案 A（推荐）：** `SystemEntity` 新增 `systemSpecData: Data?`，JSON 编码 `SystemSpec` 存进去。改动最小，不新增 entity，不动关联。
- 方案 B：新建独立 `SystemSpecEntity`，一对一关联 `SystemEntity`。结构更正，但改动较大，留作日后。

**仓储新增（NotesRepository + 协议 NotesRepositoring）：**
- `func systemSpec(systemID: UUID) throws -> SystemSpec?`
- `func updateSystemSpec(systemID: UUID, spec: SystemSpec) throws`

**传给 AI 的上下文（AIServicing.swift）：**
- `NotePayload` 新增可选字段 `systemSpec: SystemSpec?`，让 AI 知道现有技术栈，避免重复建议。

### 4.3 笔记智能排除 🔒

- 现状：Block 层级已有 `source`（manual / ai / notes）和 `deletedAt`（软删）；Note 层级有 `lastAiHash`（hash gate）。**缺**「这块要不要给 AI 看」的标记。
- **改动：** Block 层级新增 `excludedFromAI: Bool`（预设 false = 全看）。组 payload 给 AI 时，`excludedFromAI == true` 的 block 直接剔除，不送出。
- UI：该 block 加一个 🔒 小钮切换排除状态。

### 4.4 改动清单一览（约 7 个档案）

| 档案 | 改什么 |
| --- | --- |
| `DomainModels.swift` | 新增 `SystemSpec` struct；`Nudge` 扩充 `specSnapshot?`（沿用快照模式） |
| `AIServicing.swift` | `NotePayload` 加 `systemSpec?`；proposal action 加 `update_spec` |
| `PersistentModels.swift` | `SystemEntity` 加 `systemSpecData: Data?`；Block 加 `excludedFromAI: Bool` |
| `NotesRepository.swift` | 加 `systemSpec(_:)` / `updateSystemSpec(_:_:)` / `notes(in:)` |
| `NoteDocument.swift` | 加 `systemSpec` 属性 + `updateSystemSpec(_:)`（仿 `updateNudge` 写盘语义）；payload 注入 spec、剔除 🔒 block |
| `NoteChatViews.swift` | `proposalRow()` 的 switch 加 `case "update_spec"`：解析 JSON → 写入 → 标记 used |
| 新档 `SystemStructureView.swift` | 身份证只读 / 展示页 |

---

## 5. 导航变更：从「单层一页」到「四层树」

### 现状（问题）

- 首页 `NavigationStack(path:)` 直接用 `UUID` 开 `NoteScreen`，`HomeRoute` 枚举只有 `.settings`。
- 「文章 / 卡片」是 `NoteScreen` 内部的 `ViewMode` 状态切换，不是页面导航。
- 等于：点专案卡 → 直接进一个单笔记编辑页。**没有专案首页，也没有身份证、没有教练。**

### 目标（四层导航树）

```
第 1 层  专案列表（首页 HomeScreen）
            │ 点专案卡
            ▼
第 2 层  专案首页 SystemDetailScreen  ← 顶部三分页
            ├── 🤖 AI 教练  AICoachView
            ├── 📝 开发笔记  NotesListView（多笔记清单）
            │       │ 点某一篇
            │       ▼
            │   第 3 层  单篇笔记内页 NoteDetailScreen（文章/卡片在此）
            └── 🪪 系统结构  SystemStructureView（身份证）
```

### 具体改动

- **新增 `HomeRoute.systemDetail(id: UUID)`** 关联值路由；`HomeRoute` 成为**唯一路由源**，不再混用 `UUID` 直导。
- 首页专案卡点击：`path.append(HomeRoute.systemDetail(id: system.id))`。
- **新建 `SystemDetailScreen.swift`**：`@State selectedTab` 驱动三分页（`.coach` / `.notes` / `.structure`），用 `systemTabBar()` 组件（沿用 `NoteScreen` 既有 `segButton` 样式）。
- **新建 `NotesListView.swift`**：查询同 `systemID` 的笔记清单，点选进入单笔记。
- **新建 `NoteDetailScreen.swift`**：由现有 `NoteScreen` 改造，**保留全部 UI**（navBar / dock / fab / dialLayer / 文章/卡片），只移除「systemID 唯一」假设、改 `dismiss` 目标。
- **新建 `AICoachView.swift`**：读身份证＋当前/全部笔记切片，驱动教练对话。
- **新建 `SystemStructureView.swift`**：身份证只读展示。

> 既有 `ArticleView` / `CardsView` / `BlockRow` / `CardRow` **无需修改**，整组下沉到 `NoteDetailScreen` 当内容区即可。

---

## 6. AI 教练：两种玩法 + 记入结构

教练沿用既有串流（SSE）+ proposal 建议钮架构，**80% 基础设施可直接复用**（`ChatViewModel.runStream`、`Nudge` 快照、`MarkdownView`）。后端靠一个 `kickoff` 旗标与系统提示词切换两种模式。

### 6.1 引导发想（扩散模式）

**白话：** 帮你「把脑袋打开」，问开放问题，丢出一堆小字卡让你挑、让你联想，先别急着收敛。

触发：点 ⚡ 点子助攻 / 教练开场（`kickoff = true`）。

教练会围绕这几个泡泡发问（小字卡清单）：

- 🎯 **痛点**：你到底想解决什么问题？
- 📈 **市场**：谁会用？目标族群是谁？
- 🥊 **竞品**：市面上有人做了吗？他们怎么做？
- 🎬 **场景**：使用者典型的一天是怎样用到它？
- ✨ **核心功能**：先上线的 3 个功能是什么？

结尾必带 1–4 个 proposal 钮，例如：`['📇 记入结构', '✦ 优化笔记', '▦ 整理成卡片']`。

### 6.2 结构厘清（收敛模式）

**白话：** 帮你「把想法收拢成规格」，一格一格把身份证填好。

触发：发想之后，或直接进入（`kickoff = false`）。两种呈现：

- **卡片式**：教练把目前抓到的规格做成卡片让你确认 / 补充。
- **问答式**：哪个字段含糊，就追问一句把它问清楚。

当你提到硬需求（如「前端用 React」「部署在 Fly.io」），AI 侦测到 → 吐 `update_spec` proposal。

### 6.3 update_spec：把建议记入结构

- proposal 格式：`{ action: "update_spec", label: "记入结构：前端=React", instruction: <JSON 序列化的 spec 片段> }`。
- 前端：`NoteChatViews.proposalRow()` 的 switch 加 `case "update_spec"` → 解析 instruction 里的 JSON → 呼叫 `NoteDocument.updateSystemSpec()` → 标记 `proposalsUsed`（整列禁用，防重复）。
- 后端（Fly.io）：系统提示词加「硬需求侦测」逻辑，侦测到就穿插 proposal 事件。SSE 协议**不用改**，proposal 事件早已存在，只是多一个 action 值。
- **呼应第 3 节铁律：** 这是唯一写入身份证的通道，且必须「使用者点钮」才生效。

---

## 7. 省 Token 策略

目标：**AI 只在被叫时读 + 只送当前专案切片 + prompt 快取 + hash/diff 闸**，整体省 45–90% 成本。全部可在现有框架内落地，不用大重构。

### 7.1 只送切片（不要把整个专案塞进去）

- 三层上下文组织：
  - **L1 身份证**（小，约 500–1k token）：稳定，挂长快取。
  - **L2 当前笔记**（变动部分，2–10k）：编辑频繁，挂短快取。
  - **L3 跨笔记**（仅在需要时才捞，且只送摘要）：不快取。
- 🔒 排除的 block 一律不送（第 4.3 节）。
- pinned / 模组 block 不进 AI 输入。

### 7.2 Prompt 快取（Claude 官方做法）

> 依 Claude 官方文件：在 system / 内容区块上标 `cache_control: { type: "ephemeral" }`，命中快取的 `cache_read` token 只算约 0.1x 价钱，省约 90%。

- **L1 身份证**：规格稳定 → 用较长 TTL（如 1h），搭配 `lastSpecHash` 侦测变动。
- **L2 当前笔记**：编辑频繁 → 用短 TTL（5min）。
- **L3 跨笔记摘要**：结果多变 → 不快取。
- SSE `usage` 事件扩充：`{ input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens, model }`，前端可显示「本次省费 XX%」badge（`savings = (1 - actualCost/baselineCost) × 100`）。
- 实作位置：Fly.io 后端改用 Anthropic SDK，在请求体加 `cache_control`。前端 SSE 解析架构**完全相容**，只多接 usage 字段。

### 7.3 hash / diff 闸（条件命中直返，零成本）

- **整篇 hash 闸**：算当前内容 hash，跟 `lastAiHash` 比对，一样 → 直接返回上次结果 + toast「无变更，零成本」，根本不呼叫 AI。
- **块级 diff**：每个 block 带 `aiHash`，只送「新增 / 变动」的 block（标 `changed: true`），避免「影印机效应」（重复重写越漂越远）。
- **身份证 hash 闸**：新增 `lastSpecHash`，身份证没变就 skip 重新询问。
- 这套 `aiHash` + `lastAiHash` 机制**现成已实装**，只需补 `lastSpecHash`。

---

## 8. 连结 / YT 读取（排在最后面做）

**为什么排后面：** 「侦测连结」很简单（前端正则即可），但「读懂连结内容」需要后端工具配合，依赖度高，所以放最后。

**做法（沿用 Claude 官方 tool use）：**

- 三个后端工具（locked，全在 Fly.io，**绝不在 iOS app 里跑**）：
  - `find_youtube`：收 YT URL → YouTube API（或 Claude `web_fetch`）抓标题 / 时长 / 字幕 → AI 读字幕做摘要。
  - `find_github`：收 repo URL → GitHub API（OAuth token 存 Fly.io）抓 stars / 语言 / README。
  - `find_info`：收一般网址 → Claude `web_fetch`（server tool，Anthropic 端执行）抓 + 解析 + 摘要。
- 这些都用 Claude `tool_use` + JSON schema 受控输出。
- 新端点：`POST /media/fetch`（收 URL + media_type 提示）→ 回结构化 metadata + 摘要给 iOS 显示。
- proposal action 已含 `find_youtube` / `find_github` / `find_info`，前端钮**早就准备好**，等后端工具上线即可接。

**阶段三内的优先序：** 先做「侦测 + 显示连结预览占位」，真正的「读懂内容」放到这一阶段最后一刀，做不完可顺延，不挡前面验收。

---

## 9. 沿用清单（现有可直接用，不要重造）

| 现有资产 | 阶段三怎么用 |
| --- | --- |
| `notes: [NoteEntity]` 一对多 + 级联删除 | 多笔记底层，零改动 |
| `documentNote()` / `notes(in:)` | 单笔逻辑保留，清单查询备妥 |
| `RevisionEntity` + `versionPointer`（undo/redo） | 笔记 / 身份证版本快照直接用 |
| `ProposalItem`（action/label/instruction）+ `Nudge.openingProposals` 快照 | `update_spec` 直接套同一套，无需改架构 |
| `ChatViewModel.runStream()` proposal 累积管线 | 教练对话核心，原样复用 |
| `Nudge` 快照 + `nudgeFingerprint()` hash | 扩出 `specSnapshot` / `lastSpecHash` |
| `MarkdownParser` + `MarkdownView` | 教练开场白、引导泡泡渲染，100% 复用 |
| `SSEParser` + `AIEvent` enum（已含 proposal case） | 新 action 不改架构，只改资料 |
| `Block.source`（manual/ai/notes）+ `deletedAt` | 标记 AI 来源 / 软删，为智能排除铺路 |
| `hash gate`（`shouldSkipOptimize` / `changedBlockIds`） | 省钱闸现成 |
| `ArticleView` / `CardsView` / `BlockRow` / `CardRow` | 下沉到 `NoteDetailScreen`，零修改 |
| Tokens / Palette 工业美学 + `segButton` | 三分页选项卡直接套样式 |
| `NavigationPath` + `dismiss` 流程 | 新容器保持一致返回流程 |

---

## 10. 实作步骤拆分（切 4 刀，每刀可上 TestFlight）

原则：**每一刀都能独立编译、独立验收、独立上 TestFlight**，做不完不互卡。

### 🔪 第 1 刀 · 导航骨架 + 三分页空壳（地基）

- 加 `HomeRoute.systemDetail(id:)`，首页改路由。
- 新建 `SystemDetailScreen` + `systemTabBar()` 三分页。
- 三页先放空壳：教练页占位、笔记页先塞「目前单笔」、结构页占位。
- **验收：** 点专案卡 → 进专案首页，三分页可切换，旧笔记照常能编辑。
- **风险：** 路由从 UUID 改 HomeRoute 可能影响既有返回流程 → 低，集中在 `HomeScreen`。

### 🔪 第 2 刀 · 系统结构（身份证）只读 + update_spec 通道

- `DomainModels` 加 `SystemSpec`；`SystemEntity` 加 `systemSpecData`；仓储加读写。
- `SystemStructureView` 只读展示（空字段占位）。
- `NoteChatViews` proposalRow 加 `update_spec` case；`NoteDocument.updateSystemSpec()`。
- 后端补 `update_spec` 侦测 + proposal 发送。
- **验收：** 在教练 / 笔记聊到「前端 React」→ 出现「记入结构」钮 → 点 → 身份证页出现该字段。守门员铁律成立（无手动直填后门）。
- **风险：** 前后端协议需对齐（instruction JSON 格式）→ 中，需前后端同步定义。

### 🔪 第 3 刀 · 打开多笔记 + 智能排除 🔒

- `NotesListView` 多笔记清单；`NoteDetailScreen` 由 `NoteScreen` 改造（去唯一假设）。
- `NoteDocument` 支援传入 `noteID`。
- Block 加 `excludedFromAI`，🔒 切换钮，组 payload 时剔除。
- **验收：** 一个专案可建多篇笔记、互相切换；某块加 🔒 后 AI 看不到它。
- **风险：** `NoteScreen` 拆成 `NoteDetailScreen` 牵动较多 UI → 中，但因「保留全部 UI 只改 dismiss / 去唯一假设」，可控。

### 🔪 第 4 刀 · AI 教练全貌 + 省 Token 快取 +（连结读取）

- `AICoachView` 读身份证＋多笔记切片，双模式（扩散 / 收敛）。
- 后端上 prompt 快取（`cache_control`）+ `lastSpecHash`；SSE `usage` 扩充；前端省费 badge。
- 连结 / YT 读取（第 8 节）排在本刀最后，做不完顺延，不挡前面。
- **验收：** 教练能引用身份证＋多篇笔记；usage 显示 cache_read 省费；贴 YT 至少能预览。
- **风险：** 跨笔记上下文（`ProjectContext`）+ 后端工具依赖最高 → 高，故置于最后、可分次释出。

### 排期建议（粗估，可视团队调整）

| 刀 | 内容 | 粗估 | 上 TF |
| --- | --- | --- | --- |
| 1 | 导航三分页骨架 | ~3–4 天 | ✅ |
| 2 | 身份证 + update_spec 通道 | ~4–6 天 | ✅ |
| 3 | 多笔记 + 🔒 排除 | ~4–6 天 | ✅ |
| 4 | 教练全貌 + 快取 +（连结） | ~6–10 天 | ✅（分批） |

---

## 11. 与「系统身份证-设计文档」的关系

- 既有 `docs/系统身份证-设计文档.md` **聚焦字段规格**（名称 / 技术栈 / 资料库 / 上线）与 AI 建议机制（侦测 → 建议钮 → 点确认），这部分**已定案、可行、无需重构**，本文件第 4、6 节直接引用其字段表与建议流程。
- **本文件（阶段三-结构大修）统筹全景**：身份证文档没涵盖的「三分页上下层架构、AI 教练双模式、开发笔记智能特性（🔒 排除）、省 token 策略、连结/YT 后端依赖、无后门铁律的明文强化」，全在这里补齐。
- **分工总结：** 身份证文档 = 「这张证怎么长、怎么填」；本文件 = 「这张证放在哪一层、谁来填、整个房子怎么打通」。两者并存，不冲突；实作时身份证细节翻身份证文档，整体格局与排期翻本文件。

---

## 12. 开工前补洞（v2 · 4 代理勘查校准）

> 这一节是 v1 文档约 90% 成熟度的「最后一哩」补强。由 4 个代理实地读代码 + 验证后端得出，把开工前 4 个不确定点钉死，整份冲到 98% 可开工。

### 12.0 四洞结论速览

| 洞 | 结论 | 影响哪一刀 | 关键发现（白话） |
| --- | --- | --- | --- |
| 1 · 后端现况 | 确认可控 ✅ | 第 2、4 刀 | **后端是好的、能跑能部署**（package.json/fly.toml/Dockerfile 全在，旧记忆「缺 package.json」已澄清）。前端 proposal/cache_control/三层 system 都现成。唯一真风险：SDK 0.39 太旧，连结工具（web_fetch）要先升级 SDK——只影响第 4 刀。 |
| 2 · update_spec 契约 | 确认可控 ✅ | 第 2 刀 | proposal 的 instruction 在线路上是「字串」，所以 spec 要 **stringify 成 JSON 字串**塞进去，不改解析器。已给完整 JSON 范例 + SystemSpec 型别 + 合并规则（null=不动、apis 整阵列替换）。 |
| 3 · 拆分摩擦 | 确认可控 ✅ | 第 3 刀 | 「保留全部 UI」大致成立（内容/聊天视图早就只绑 doc）。真摩擦在两处：NoteDocument 要从 systemID 改 noteID；`cleanupEmptyOnLeave` 会 deleteSystem，多笔记下**必须改成 deleteNote**（最易踩的回归点）。工作量：中。 |
| 4 · 教练多笔记 | 确认可控 ✅ | 第 4 刀 | 三层切片：身份证 + 当前笔记全文 + 其他笔记「前 120 字」短摘要（直接复用现成 `snippet()`，不另造算法、不烧 token 二次摘要）。给了 token 预算表与超额裁剪顺序（当前笔记永不裁）。 |

---

### 12.1 洞1 · 后端改动落点 + 现况 + 风险 + 补法（第2刀 / 第4刀）

#### 后端现况（结论：能跑、能改 prompt、能部署）
- 后端在 `server/`，单一入口 `server/src/index.js`（348 行）。用 **Anthropic 官方 Node SDK `@anthropic-ai/sdk`（裸 HTTP 没用）**。
- **package.json 存在，过往「缺 package.json」疑虑已解除**：`server/package.json`（main=src/index.js, start=node src/index.js, dep=@anthropic-ai/sdk ^0.39.0）+ `server/package-lock.json` 都在，**不会挡部署**。
- 部署三件套齐全：`server/fly.toml`（app=brainstrom-ai / nrt / port 3000 / min_machines_running=1）、`server/Dockerfile`（node:20-slim）、`server/README-deploy.md`（fly launch→secrets→deploy→health 步骤完整）。
- 现成机制：SSE 串流（`sse()` :32、`messages.stream()` :278）、proposal 事件（`PROPOSE_TOOL` :40 + emit proposal :303）、tool_use 强制 JSON（`tool_choice:{type:'tool'}` :191/:234）、三层 system + cache_control（:85/:190/:233）。

#### 第2刀 · update_spec proposal（落点：只动 `server/src/index.js`，容易）
- `PROPOSE_ACTIONS`（:40）已含 5 个 action，**新增 `'update_spec'` 进 enum** 即可（前端钮早已准备）。
- 在 `buildSystem()` 的任务指令层（COACH_* / CHAT_TASK，:60-71）**加「硬需求侦测」规则**：侦测到名称/技术栈/资料库/上线等硬需求 → 呼叫 propose 抛 `{action:'update_spec', label:'记入结构：前端=React', args.instruction:<JSON 序列化 spec 片段>}`。
- proposal 校验段（:293-304）目前只放行 `args.instruction` 一个栏位——`update_spec` 把 spec JSON 塞进 `instruction` 即可复用，**无需改 schema**。SSE 协议不用改。

#### 第4刀 · prompt 快取 + 连结工具（落点：`server/src/index.js`；连结工具另需升级 SDK + 新端点）
- **prompt 快取**：cache_control:{type:'ephemeral'} 三处已挂；要做 L1 长 TTL 只需把身份证那层加 `ttl:'1h'`（语法已验证）。`usage` 事件（:157-161）已带 cache_read_input_tokens，要补 `cache_creation_input_tokens` 给前端算省费 badge——**小改**。
- **连结工具（排最后）**：要新开 `POST /media/fetch` 端点 + 接 YouTube API/GitHub API（token 存 fly secrets）+ Claude `web_fetch` server tool。

#### 风险与补法
- **SDK 0.39.0 过旧**（现行约 0.88+）：`web_fetch` server tool 在 0.39 用不了。**第4刀的 find_info 开工前先 `npm i @anthropic-ai/sdk@latest` 升级、跑一次 health 验证**；第2刀/快取不受影响、不用升。
- **部署补法**：package.json/lock 都在，`cd server && fly deploy` 即可；改完 prompt 不需改前端、不需改 SSE 协议。
- **上线前**：使用者本地 `curl https://brainstrom-ai.fly.dev/ai/health` 确认机器在线、Fly secrets（ANTHROPIC_API_KEY / AUTH_TOKEN）仍在（沙箱无网络，本次未实测）。

---

### 12.2 洞2 · update_spec（「记入身份证」钮）前后端契约（第2刀）

#### 核心约束（决定一切）
SSEParser 把 proposal item 的 `args.instruction` 当 **String** 取出，塞进 `ProposalItem.instruction: String?`。因此 **update_spec 的结构化资料必须 stringify 成一段 JSON 字串**，放在 `args.instruction` 里。**不改 SSEParser、不改 ProposalItem 型别**——零侵入复用既有 proposal 管线。

#### SSE proposal 事件原文（后端 → App，wire 格式）
```
data: {"type":"proposal","items":[{"action":"update_spec","label":"📇 记入身份证","args":{"instruction":"{\"name\":null,\"frontend\":\"React\",\"backend\":null,\"apis\":[\"Stripe\"],\"database\":\"PostgreSQL\",\"server\":\"Fly.io\",\"deployMethod\":null}"}}]}
```
- 整笔事件是 **一行 `data:` + 空行**（逐行扫，空行 flush）。
- `args.instruction` 的值是 **被转义的 JSON 字串**（里面的 `"` 要 `\"`），不是 nested object——wire 上的硬约束。
- `label` ≤12 字，建议固定前缀 `📇 记入身份证`，可按字段定制如 `📇 记入:前端=React`。

#### instruction 内 JSON（解开转义后）= SpecPatch（部分更新补丁）
字段对齐设计文档第一版（name / frontend / backend / apis[] / database / server / deployMethod，全选填）。规则写死：
- **null 或缺键 = 此次不动该字段**（保留旧值）；**非 null = 覆盖该字段**。语义是**部分更新（merge），不是整包覆盖**。
- `apis` 给了就**整阵列替换**（非逐项 append）；后端若只想加一个 API，应把「合并后的完整 apis 阵列」一起给。
- 后端不得发空补丁（7 字段全 null）；至少 1 个非 null 才发钮。

#### SystemSpec 型别（前端新建，目前 0 存在）
```swift
struct SystemSpec: Equatable, Codable, Sendable {
    var name: String?
    var frontend: String?
    var backend: String?
    var apis: [String] = []
    var database: String?
    var server: String?
    var deployMethod: String?
}
```
持久化挂在「系统」层级（SwiftData，与自由笔记并存），非塞进 Note.blocks。

#### 前端解析→写入→标记 used（写死流程）
在 NoteChatViews.swift proposalRow 的 switch 增加 case，**update_spec 不可加进 locked 集合**（否则被当「即將推出」拦掉）：
```swift
case "update_spec":
    noteVM?.applySpecPatch(doc, instructionJSON: item.instruction)
```
ViewModel 端解析与合并（null/缺键不动）：
```swift
func applySpecPatch(_ doc: NoteDocument, instructionJSON: String?) {
    guard let json = instructionJSON?.data(using: .utf8),
          let p = try? JSONDecoder().decode(SpecPatch.self, from: json) else {
        toast.show(String(localized: "身份证资料读取失败")); return
    }
    var spec = doc.systemSpec
    if let v = p.name        { spec.name = v }
    if let v = p.frontend    { spec.frontend = v }
    if let v = p.backend     { spec.backend = v }
    if let v = p.apis        { spec.apis = v }      // 整阵列替换
    if let v = p.database    { spec.database = v }
    if let v = p.server      { spec.server = v }
    if let v = p.deployMethod{ spec.deployMethod = v }
    doc.updateSystemSpec(spec)                       // 持久化
    toast.show(String(localized: "已记入身份证"))
}
private struct SpecPatch: Decodable {
    var name, frontend, backend, database, server, deployMethod: String?
    var apis: [String]?
}
```
**标记 used 免写**：proposalRow 进 switch 前已呼叫 `markProposalsUsed(bubble.id)`，整列立即变灰 disabled，天然防重复记入。

#### 后端发钮时机 + 注意
- chatNote/optimize/structure 任一流中，Claude 侦测到使用者陈述硬需求（技术栈/资料库/部署）→ 在 done 之前发一笔 proposal 事件。
- **第一版后端一次只发 1 个 update_spec item**（见下方 openRisk：整列禁用与「批量个别取消」冲突）。
- update_spec 是**唯一 instruction 内放 JSON 的 action**（edit_text 的 instruction 是纯文字），前端解析务必 `try?` + 失败给 toast 不崩。

**⚠️ 待确认（前后端拍板）**：
1. 「一批建议可个别取消」与现有「整列禁用」冲突 → 第一版只发 1 个 item；要批量需把 `proposalsUsed: Bool` 改 `Set<Int>`（额外前端工）。
2. proposalRow 只渲染前 4 个 item，update_spec 要排阵列前面别被截掉。
3. AI 反向读身份证当上下文 → 要给 optimize/structure/chatNote 三个 Body 加 spec 字段（后端线）。

---

### 12.3 洞3 · NoteScreen → NoteDetailScreen 拆分清单（第3刀）

#### 状态归属表
| 现 NoteScreen 成员 | 拆后归属 | 理由 |
|---|---|---|
| `let systemID: UUID` | 容器 SystemDetailScreen | 系统级标识，三分页共用 |
| `@Environment root/palette` | 两边都要 | 容器导航 + 内页业务都用 |
| `@Environment(\.dismiss)` | 容器（返首页）+ 内页（返容器） | 两个 dismiss 含义不同（见下） |
| `doc / noteVM / chatVM` | 内页 NoteDetailScreen | 绑单笔，随笔创建/abort/reset |
| `showDial/showChat/chatInput/chatFocused` | 内页 | 全是单笔 UI 局部态 |
| 容器需新增 | `selectedTab + notes:[Note]` | 三分页 + 笔记清单 |

ArticleView/CardsView/BlockRow/CardRow、NoteViewModel/ChatViewModel **无需改归属**——只绑 `@Bindable doc`，整片随内页搬。

#### 去「系统内笔记唯一」假设
1. `NoteDocument(systemID:)` → 改 `NoteDocument(noteID:)`。
2. NoteDocument init 内部用 `repository.note(id:)`（已存在）取单笔，不再走 `documentNote(for:)` 的 `.first`。
3. 容器列清单改用 `notes(in: systemID)`（已存在，已按 updatedAt 倒序）。
4. 数据层早已多笔就绪，本步只让 NoteDocument 不再假设唯一。

#### dismiss 改法（最易出 bug 处）
- 内页返回钮 `dismiss()` → 回容器的笔记分页（pop 一层），**不再回首页**。
- `deleteSystem()` 是系统级动作 → 上移容器；内页只保留「删本笔记」`deleteNote(id:)`。
- **⚠️ 最易踩回归点**：`cleanupEmptyOnLeave` 内部目前会 `deleteSystem`，多笔记下**必须改成 deleteNote**，否则删一笔空笔记会误删整个系统。
- HomeScreen 的 `navigationDestination(for: UUID.self)` 目标从 NoteScreen 改成 SystemDetailScreen。

#### 真实工作量：中
- 低：UI 零改（已解耦），纯搬文件 + 改 extension 目标名（`extension NoteScreen` → `extension NoteDetailScreen`）。
- 中：NoteDocument init 契约变更牵动 documentNote/cleanupEmptyOnLeave/snippet 三处「取最新一笔」逻辑；新建容器 + 三分页骨架；双层 dismiss 语义梳理。
- 不构成高：无数据迁移、无后端契约改动、无 UI 重画。

---

### 12.4 洞4 · 教练多笔记切片 + token 预算（第4刀，补强第 7 节）

#### ProjectContext 结构（新增，放 AIServicing.swift）
```swift
/// 教练专用:整个专案的三层上下文切片。单笔记 optimize/structure 不用这个,继续用 NotePayload。
struct ProjectContext: Equatable, Codable, Sendable {
    let spec: SystemSpec?            // L1 身份证(稳定,挂长快取),nil 时整段省略
    let currentNote: NotePayload     // L2 当前笔记全文(挂短快取),复用既有 NotePayload
    let otherNotes: [NoteDigest]     // L3 其他笔记短摘要(不快取),已做预算裁剪
    struct NoteDigest: Equatable, Codable, Sendable {
        let id: String; let title: String; let summary: String
        let updatedAt: Date; let truncated: Bool
    }
}
```
`chatNote` 加**可选**参数（旧调用零改动）：`chatNote(messages:note:project: ProjectContext? = nil, kickoff:)`。

#### L3 摘要怎么取（不另造算法）
**直接复用 `NotesRepository.snippet(of:)`**（取第一个非空 paragraph/heading，截断），教练场景上限放宽到 **120 字**（约 60–90 token），前缀拼 title。
- 不用 fnv hash（指纹不可读、对 AI 无语义）；不做 AI 二次摘要（反向烧 token）。
- 跳过 🔒/pinned/module 块；现算即可（N≤8 篇、每篇扫到第一段就停），无需落盘缓存。

#### token 预算表（按「1 中文字≈1 token」保守估）
| 层 | 内容 | 软上限 | 硬上限 | 快取 |
| --- | --- | --- | --- | --- |
| L1 身份证 | SystemSpec | 800 | 1,000 | ephemeral 长 TTL(1h) |
| L2 当前笔记 | 全文(去 🔒/pinned/module) | 8,000 | 10,000 | ephemeral 短 TTL(5min) |
| L3 其他笔记摘要 | NoteDigest × N | 1,500 | 2,000 | 不快取 |
| 历史对话 | messages | 2,000 | 3,000 | 不快取 |
| 系统提示词 | 教练人格+模式 | 1,200 | 1,500 | 随 L1 挂长快取 |
| **输入合计** | | **~13.5k** | **17.5k** | |

#### 超预算裁剪顺序（在 App 端组好，别让后端裁）
铁律：**当前笔记 L2 永不裁**。由低到高裁：① L3 其他笔记（按 updatedAt 取最近 N=8 篇，逐篇累加超 2,000 就停、被截设 `truncated=true`）→ ② 历史对话（超 3,000 从最旧丢，保留 kickoff 首条）→ ③ L2 仅当单篇 >10k 才退回 diff/changed → ④ L1 不裁。

#### 落点（旧路径不动）
| 落点 | 改什么 |
| --- | --- |
| AIServicing.swift | 加 `ProjectContext`；`chatNote` 加 `project? = nil` |
| AIServiceLive.swift | chatNote Body 加可选 `project`，nil 不编码 |
| NoteDocument.swift | 加 `projectContext(repository:)`：L1 读 spec、L2 现有 payload、L3 调 `notes(in:)` 取前 8 篇做 digest |
| NotesRepository.swift | snippet 抽成 `digest(of:maxChars:)` 复用 |
| NoteAIViewModels.swift | runStream 加 `project?` 形参；**教练页走带 project 路径，单笔记页聊天传 nil** |

后端只需认 `project` 字段、L1/系统提示词标 cache_control、L3 不标快取。SSE 协议不动。

**⚠️ 依赖**：L1（SystemSpec）与 🔒（excludedFromAI）要等第 2/3 刀先落地；在那之前 spec 传 nil，教练只有 L2+L3。token 上限是保守默认值，待后端 `usage`/`count_tokens` 实测微调。

---

---

## 13. 新建流程重构 + 主笔记锚点（v3 · 2026-06-12 使用者裁定）

> 背景：build 1-5 把三分页、身份证、多笔记、教练、省 token 都做出来了。但实测发现两个流程问题，使用者裁定重构本节。

### 13.0 两个问题（为什么要改）

1. **取名流程层级错了**：现在是「进单篇笔记 → 命名闸卡住 → 取名 → ⚡名称触发教练开场」。取名应该提到**建专案的最上层**，建完直接进**专案级 AI 教练**聊。
2. **没有真正的「主笔记」**：`documentNote` = `notes.sorted{updatedAt}.first` = **「最近编辑的那篇」**（NotesRepository.swift:49）。系统结构、AI 教练、▦结构化 三处都锚定它 → 你一碰子笔记，系统结构的卡片和教练就**跟着飘**。`NoteEntity` 只有 updatedAt、**无 createdAt**，连「最早那篇」都判不出。

### 13.1 新流程（一句话 + 图）

**一句话：一个专案 = 一篇「主笔记」(置顶、装身份证) + 若干「子笔记」(随手写)。取名在最上层，建完直接进教练聊，所有结构/教练锚定主笔记、永不飘。**

```
按 ＋  →  弹窗:输入「系统名称 或 一个灵感」  →  建系统 + 主笔记(置顶,标题=输入) + 系统记 primaryNoteID
        →  跳专案首页,默认停「🤖 AI 教练」分页,自动开场(拿名称/灵感当起点)
        →  教练每则回复可:① 📝 加入笔记(写进主笔记)  ② 📇 记入结构(写身份证)
   ┌──────────────────────┬──────────────────────────┬─────────────────────────┐
   ▼                      ▼                          ▼
🤖 AI 教练(锚主笔记+看全部笔记摘要)   📝 开发笔记(主笔记置顶+子笔记)   🪪 系统结构(=主笔记封面)
   聊→加入笔记/记入结构              点进去纯写作+底部AI聊天          身份证(SystemSpec) + 主笔记的结构卡片
```

### 13.2 主笔记锚点（核心修正）

- **`SystemEntity` 新增 `primaryNoteID: UUID?`**：指向该系统的主笔记。建专案时设定，从此固定不飘。
- **`documentNote(for:)` 改写**：优先回 `primaryNoteID` 指向的笔记；primaryNoteID 为 nil（旧资料）→ 回最近编辑那篇并**顺手回填 primaryNoteID**（平顺迁移，不炸旧资料）。
- **锚点统一**：SystemStructureView（系统结构）、AICoachView（教练）、▦结构化 全部锚定主笔记（即 documentNote 改写后的稳定值），不再随「最近编辑」飘。
- **身份证 = 主笔记封面**：SystemSpec 仍存系统层级（SystemEntity.systemSpecData，沿用第 4 节），但**呈现**在「系统结构」分页顶部，下接主笔记的结构卡片 → 概念上「主笔记最顶端那张卡 = 身份证」。
- **开发笔记清单**：主笔记**置顶**并挂「主」徽章；子笔记排下面。

### 13.3 删除清单（旧「取名 + 笔记层⚡开场」）

| 删什么 | 落点 |
| --- | --- |
| 命名闸「先取名才能写」整套 | NoteDocument：`naming` 属性、`isNamingGate`、`quickName`、命名态分支；persist/applySnapshot 里重算 naming |
| 命名提示虚线框 + 「先随便取 / ✓确认取名」钮 | NoteContentViews：ArticleView 的 `if doc.naming` 区块、titleAux 命名态部分、titleReady |
| 笔记里的「⚡ 让 AI 教练看看」胶囊 | NoteContentViews：titleAux 的 nudge 胶囊（line 130-151）；NoteScreen 的 onKickoff 闭包；ArticleView 的 `onKickoff` 参数 |
| 笔记层 ⚡开场/重播（nudge 快照那套） | NoteAIViewModels：`startKickoff`（名称触发版）、`sparkReplay`、runStream 的 `saveNudge`/`kickoff` 耦合 |
| 旧首页 banner 说词 | HomeScreen banner |
| dock 的 `doc.naming` 禁用判断 | NoteScreen dock / fab |

> **保留 `Nudge` 资料字段不删**（NoteDocument.nudge / NoteEntity.nudgeData）→ 旧资料 decode 不会失败；只是不再写入/使用。

### 13.4 保留清单（使用者明令不准动）✅

- **每篇笔记底部「问 AI·这则笔记」聊天面板**（NoteChatViews 全部：chatPanel/sendChat/chatList/chatBubbleView/proposalRow）→ 不动。
- **单篇笔记编辑能力**：标题输入、续写、块编辑（editBlockText/toggleTodo/move/delete/appendFromContinue/insertModule）、✦优化、🗑、↶↷、🔒排除 → 全保留。
- 三分页容器、系统结构页（身份证+卡片）、ChatViewModel 的一般串流（kickoff=false 路径）→ 不动。
- 教练层的 kickoff（专案级开场）改成「不依赖 nudge、进来自动开场」，但串流核心复用。

### 13.5 新能力「加入笔记」（先做基础版，使用者说先不设太多）

- 教练回复下方加钮 **「📝 加入笔记」** → 把该则内容用现成 `appendFromContinue`（追加进主笔记）或 `createNote`（另开）写入。落点：AICoachView.proposalRow/tapProposal + NoteAIViewModels 新增 `addCoachReplyToNote(text:doc:)`。
- **「📇 记入结构」** → 已有（update_spec，第 12.2 节）。
- 「总结后加入」→ 基础版先做「整则加入」；总结版后续再加。
- proposal action 视需要新增 `add_to_note`（后端可在教练回复时附带；或前端就地用气泡文本，不必走后端——基础版优先前端就地，零后端改动）。

### 13.6 后端配套（server/src/index.js，小改）

- 现有 `buildSystem(note, kickoff, project)` + COACH_EMPTY/CONTENT_TASK 分流**够用**：专案级教练开场把「名称/灵感」当首条 user 讯息（前端注入），kickoff=true 即走教练开场路径。
- **建议简化**：教练开场不再按「笔记有没有内容」分流（新流程刚建专案、主笔记空），统一走一套「专案级教练开场」prompt（拿名称/灵感开场、问定位问题、结尾抛 proposal）。SSE 协议、proposal、update_spec、cache 全部不动。
- `add_to_note` 若走后端：proposal 加该 action；基础版可不走后端（前端就地加），**优先零后端改动**。

### 13.7 数据迁移与风险

- **旧系统（build 5 已建）无 primaryNoteID**：首次 `documentNote` 调用时回最近笔记并回填 primaryNoteID → 平顺，不炸。
- **空专案清理冲突**：新流程建专案即种主笔记（noteCount≥1）→ 首页 reload 的「noteCount==0 清理」不会误删。主笔记标题=名称/灵感，不是空名。
- **命名态移除**：标题输入框**保留**（仍可改主笔记/子笔记标题），只删「卡住不能写」的闸 + 名称触发开场。
- **教练默认分页**：SystemDetailScreen 初始 tab 从 `.notes` 改 `.coach`；ZStack 三页同挂载，切换无碍。
- **saveNote 的 renameSystem**：目前笔记标题→系统名同步（NoteDocument.saveNote）。主笔记标题→系统名 OK；子笔记标题**不应**改系统名 → saveNote 改成「只有主笔记才 renameSystem」。

### 13.8 实作步骤（切 5 步，每步可编译+测+可上 TestFlight）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| A · 主笔记锚点 | SystemEntity 加 primaryNoteID；documentNote 改写+回填；saveNote 只主笔记 renameSystem | 单测：建系统→primaryNoteID 稳定；编子笔记不改主笔记 |
| B · 建专案弹窗 | HomeScreen ＋/建立钮 → 弹窗输入名称/灵感 → createSystem(name)+createNote(主笔记)+set primaryNoteID → push systemDetail | 点＋出弹窗、输入后进专案 |
| C · 默认进教练+自动开场 | SystemDetailScreen 默认 .coach + 传 autoKickoff；AICoachView onAppear 自动开场（无历史时） | 建完直接看到教练开口 |
| D · 删命名闸+⚡开场 | 删 13.3 清单；保留 13.4 清单；标题输入框留 | 笔记直接能写、底部聊天仍在、编辑全在 |
| E · 加入笔记 + 主笔记置顶徽章 + 后端简化 | AICoachView「📝加入笔记」；NotesListView 主笔记置顶徽章；后端教练开场 prompt 简化 | 教练回复能加进主笔记；列表主笔记置顶 |

> 每步：build_sim 绿 → test_sim 全过（E2E 重接新流程）→ 必要时截图 → 下一步。全部完成才 archive 上 TestFlight build 6。

---

### 13.9 实作细节补强（v3 · 7 代理验证后钉死，冲 98%）

> 验证说明：验证工作流平均信心 71%、4「冲突」——经查 **几乎全是「文档描述未来改动 vs 现状代码」的测量假象**（修复文档本就该与现状不同）。后端代理 92%（基本不用改）。以下是代理挖出的 **真正要先钉死的 8 个实作细节**，补齐后文档达可照做 98%。

1. **⚡重播钮耦合（最关键，否则编译失败）**：`NoteChatViews.swift:16-24` 的 ⚡重播钮调用 `chatVM?.sparkReplay(doc)` 并读 `doc.nudge.state`。删 sparkReplay 会让它悬空。**解法：从 chatPanel 拿掉整个 ⚡重播钮区块（它属 kickoff/nudge 系统）**，chatPanel 其余（输入框/送出/停止/气泡/proposal）**全保留**。这就是「保留聊天面板但拆掉 kickoff 耦合」的精确做法。
2. **建专案原子化**：弹窗确认后须 **①createSystem(name) ②createNote(in:title:name) ③设 primaryNoteID ④push**，最好用仓储一个组合方法 `createSystemWithPrimaryNote(name:) -> (system, note)` 一次落盘，避免「只建了系统没建笔记」的崩溃窗口。
3. **saveNote 的 renameSystem 加条件**：`NoteDocument.saveNote:260` 现无条件 `renameSystem`。改成 **只有主笔记才同步系统名**：`if noteID == (try? repository.primaryNoteID(for: systemID)) { renameSystem(...) }`。仓储新增 `primaryNoteID(for:) -> UUID?`。
4. **documentNote 改写（确定写法）**：
   ```swift
   func documentNote(for systemID: UUID) throws -> Note {
     guard let system = try fetchSystem(id: systemID) else { throw RepositoryError.systemNotFound }
     if let pid = system.primaryNoteID, let p = system.notes.first(where: { $0.id == pid }) { return try toDomain(p) }
     if let recent = system.notes.sorted(by: { $0.updatedAt > $1.updatedAt }).first {
       system.primaryNoteID = recent.id; try context.save(); return try toDomain(recent)   // 回填迁移
     }
     return try createNote(in: systemID, title: system.name)   // 兜底
   }
   ```
   SwiftData 的 `context.save()` 本身是 transaction，回填并发安全。
5. **autoKickoff 防 ZStack 三页齐触发**：三分页 ZStack 同时挂载 → 三个 onAppear 都会触发。AICoachView 的自动开场必须 **三重闸**：`autoKickoff == true && messages.isEmpty && !streaming`，且**只在 coach 分页 active 时**触发（把 `active: tab == .coach` 传进去，仿 SystemStructureView）。开场前置作业（读 projectContext）放进 kickoff 内即可。
6. **后端不用改（重点澄清）**：现有 `handleChatNote` 在 kickoff 且主笔记空时走 `COACH_EMPTY_TASK`——它正是「问定位问题(谁用/痛点/最小范围)」的发想开场，**刚好符合新流程**。唯一要做的是 **前端把使用者输入的「名称/灵感」当第一条 user 讯息注入**（取代后端 line 309 的硬编码占位语）：AICoachView.kickoff 时 `runStream(history: [ChatMessage(role:.user, content: 名称/灵感)], kickoff: true)`。`COACH_EMPTY/CONTENT` 分流可不动；SSE/optimize/structure/update_spec **零改动**。
7. **加入笔记＝前端就地（零后端）**：教练气泡下「📝加入笔记」→ 直接 `mainDoc.appendFromContinue(气泡文本)` 写进主笔记（mainDoc = documentNote）。**不需要** add_to_note proposal、不需改 PROPOSE_ACTIONS。
8. **snippet 不改 + 主徽章**：`NotesRepository.snippet` 维持「取最近」（首页卡摘要直观）。`NotesListView` 把主笔记**置顶**并挂「主」徽章（用 `primaryNoteID(for:)` 判定）。删主笔记的边界：删后 primaryNoteID 悬空 → 下次 documentNote 回填最近那篇；若无笔记 → 首页 reload 清空系统。

**E2E 重接（步骤 E）**：旧 `home.create→noteslist.create→note.quickname` 会因删命名闸而断。新流程：`home.create →（弹窗 home.projectNameInput 输入）→ 确认 → 默认 coach 分页自动开场 → 教练回复出现 → 📝加入笔记 → 切系统结构 structure.run → 看卡 → systemDetail.back 回首页`。删除对 `note.quickname/note.confirmname/dock.structure` 的依赖。

---

---

## 14. build 7 · 身份证扩充 + 引导式选项访谈 + B模式侦测 + 竞品API（v4 · 2026-06-12）

> 12 代理扫描后端+iOS、对照最终共识产出。本节把所有「实作选择题」一次拍定（工程默认值），供照做。
> 产品方向(使用者已拍板)见本节 14.0；工程决策见 14.1 起。

### 14.0 产品共识（使用者已拍板，不再讨论）
- 流程：＋→弹窗①输名称/灵感→弹窗②选「进AI引导/之后慢慢填」。引导→教练选项式访谈；慢慢→开发笔记自由写，AI 背后侦测。两条路填同一张身份证；B 模式留「开始引导」入口。
- 身份证 5 区~13 格（皆选填）：①一句话⭐/目标用户⭐ ②痛点⭐/核心价值 ③竞品/市场策略/商业模式 ④核心功能⭐ ⑤前端/后端/API/资料库/服务器/部署。⭐核心4=一句话/目标用户/痛点/核心功能。
- 守门员：只有 AI 能写、人不能手动直接改；一律「AI 建议→点确认」才生效；AI 改已确认字段要先问（点了才覆盖）。
- 引导：一次一题+进度「X/核心4」+每题 3-4 选项钮+「自己说」；核心4填齐→转推敲开发；先理念后技术。
- B 模式：核心4填完前，教练+每篇笔记底部聊天都侦测、建议记入（第一要务，非强制）；填完回归正常。
- 竞品：build 7 就做（资讯够=一句话+痛点+目标用户）；免费 iTunes + GitHub Search API，后端直接 HTTP，不升 SDK；LLM 只出关键字+摘要；回真实卡片点选记入。常驻钮/完整竞品模组留模组阶段。

### 14.1 工程决策（拍定 12 代理挖出的选择题）
1. **身份证两态简化**：每次写入都是「使用者点 proposal 确认」才生效＝本就是「已确认」。故 **不做独立的「待确认」持久态**；模型用 `SystemSpec.confirmedFields: Set<String>`（已填字段集）。身份证页：已填=橘色显示、空=淡色占位。**AI 改已确认字段**＝backend 的 proposal label 明示「更新：旧→新」，点了才覆盖（这就是「先问」）。— 满足共识、最省工。
2. **选项钮＝多个 update_spec proposal item**（各带一个候选值的 JSON），复用现有 proposal 框架（≤4 项），**不改 schema**。「自己说」＝不另做钮，使用者直接在输入框打字（下一轮 chatNote）。
3. **进度「X/核心4」＝前端算**（从 SystemSpec 核心4填了几格），coach header 显示；**后端不加进度字段**。
4. **引导/慢慢模式**：chatNote payload 加 `mode: "guided"|"free"`。backend `mode=guided`→走 GUIDED_INTERVIEW_PROMPT（一次一题+出选项）；`free`→沿用现有 COACH + SPEC_DETECT 侦测。
5. **B 模式侦测**：单笔记底部聊天的 `runStream` 也带 `project:{spec}`；backend SPEC_DETECT 扩到侦测理念字段（痛点/目标用户/…）。note 聊天的 proposalRow **早已支援 update_spec**（第12.2节），所以前端几乎不用改。「核心4优先」＝backend prompt 软性加权（非禁用）。
6. **竞品端点**：新 `POST /find/competitors {projectBrief, sources}`。backend：小 LLM 抽 2-3 关键字 → 打 iTunes(`itunes.apple.com/search?entity=software`)＋GitHub(`api.github.com/search/repositories`，token 选用) → 去重 → 回 JSON `{items:[{source,title,url,subtitle,score}]}`。**非 SSE、纯 HTTP、不升 SDK**。触发：前端在核心3(一句话+痛点+目标用户)填齐后，AICoachView 自动浮一颗「🔍 帮你找竞品」→点了才打。
7. **竞品资料**：`SystemSpec.competitors: [CompetitorItem]`（{source,title,url,note?}）。点竞品卡→append 记入。
8. **mode 参数传递**：`HomeRoute.systemDetail(id, mode: CreationMode)`，`CreationMode={kickoff,notes}`；SystemDetailScreen `init(mode:)` 决定初始 tab + 是否自动开场。

### 14.2 资料模型变更（DomainModels / SystemSpec）
- `SystemSpec` 加字段（皆 optional String，除 competitors）：`oneLiner / targetUser / painPoint / coreValue / marketStrategy / businessModel / coreFeatures`；`competitors: [CompetitorItem]`；`confirmedFields: [String]`（已确认字段键集，编码进 JSON）。
- 新 `struct CompetitorItem: Codable,Equatable,Sendable { source(app_store|github); title; url; subtitle?; note? }`。
- `var coreFilledCount: Int`（计算属性，0-4）＋`var coreComplete: Bool`；`var infoEnoughForCompetitors: Bool`（一句话&&痛点&&目标用户）。
- 向后相容解码：缺新键→默认 nil/[]（旧资料不炸）。
- `SpecPatch` 同步加上述字段 + 合并规则不变（null/缺键不动；点 proposal 时把该字段键加进 confirmedFields）。

### 14.3 实作步骤（切 7 刀，每刀编译+测；全绿才上 build 7）
| 刀 | 内容 | 验收 |
| --- | --- | --- |
| 1 · 模型 | SystemSpec 扩 13 字段+CompetitorItem+confirmedFields+coreFilledCount；SpecPatch 同步；单测 +N | 单测过；旧资料解码不炸 |
| 2 · 后端访谈 | GUIDED_INTERVIEW_PROMPT + chatNote 收 mode + SPEC_DETECT 扩理念字段 + 核心4软加权；deploy；curl 验证出选项 | curl guided 回「一题+≤4选项」 |
| 3 · 建专案双弹窗 | HomeScreen 弹窗①→②；HomeRoute.systemDetail(mode)；SystemDetailScreen init(mode) | 点＋→输名→选模式→落对页 |
| 4 · 引导访谈UI | AICoachView：进度X/4、选项钮、核心填齐转开发、「开始引导」入口 | 访谈一次一题、点选项记入 |
| 5 · B侦测+结构页 | note 聊天带 spec；SystemStructureView 5 区+进度+已填态 | 笔记聊到痛点跳记入钮；结构页 5 区 |
| 6 · 竞品 | 后端 /find/competitors（iTunes+GitHub）；AICoachView「🔍找竞品」+竞品卡+记入 | 核心3齐→找竞品→点卡记入 |
| 7 · 验收 | E2E 重接新流程；65+ 测全绿；bump build 7；archive 上传 | 全绿 + UPLOAD SUCCEEDED |

### 14.4 回归底线（不可破）
- 现有四流(optimize/structure/单笔记chat/教练)不破；SSE 协议不动（竞品走新 HTTP 端点）。
- 单篇笔记编辑 + 底部聊天面板完整保留；守门员（人不能直接改身份证）维持。
- 主笔记锚点不动；身份证仍是系统层级、主笔记封面。

---

---

## 15. build 9 · 引导体验修补（v5 · 2026-06-13；12探勘+6提案6驳斥对抗后定案）

> 来源：build 8 实测两问题（点选项 AI 不接话 / 竞品搜寻无关）→ 12 探勘代理挖根因 → 6 提案+6 驳斥对抗，挖出比初版更好的修法。本节为定案。

### 15.1 问题①：引导访谈点选项后 AI 不接话（直接修，无更好方法）
- **根因**：`AICoachView.tapProposal` 点 update_spec 只做 `applySpecPatch + markProposalsUsed`，没把选项变使用者回答、没触发下一题。后端 guided「算下一题」逻辑现成（`buildSystem` coreOrder.find），只差前端把答案送回去。
- **修法**：教练里点 update_spec 选项 → ①把候选 label append 成 user 气泡 ②applySpecPatch 记入 ③`chatVM.send(label, project: doc.projectContext(), mode:"guided")` 触发下一题。
- **必处理的 3 个安全锁（驳斥代理挖出）**：
  1. **记录失败不续聊**：`applySpecPatch` 改成回 `Bool`（解析/写入成功才回 true）；**只有成功才 send**，否则 AI 拿旧 spec 重复问同题。
  2. **streaming 时锁选项**：`proposalRow` 的选项钮在 `chatVM.streaming==true` 时 disabled（现仅看 proposalsUsed）；否则点了被 `send` 的 `!streaming` guard 静默丢，造成「进度动了但没回应、还永久变灰」更糟态。
  3. **教练续聊 / 笔记只记录 分流**：`AICoachView`（引导）点 update_spec → 续聊；`NoteChatViews`（单笔记聊天）点 update_spec → 只记录、**不续聊**（那是问答非访谈）。两处逻辑分开。

### 15.2 问题②：竞品搜寻无关（对抗后找到更好方法，已与使用者确认采用）
- **🔑 最大根因（比关键字更深）**：**竞品 ≠ 开源 repo**。iTunes(App Store)=商业竞品；GitHub repo=可参考/借用的开源，不是竞争对手。build 8 把两者混在同一排「竞品」→ repo 被当竞品，看起来乱。
- **次根因**：拿整句长「一句话」当搜寻词 → 引擎被长句搞混，回行事历/书。
- **定案修法（零额外 LLM，比初版 Haiku 抽词更省更准）**：
  1. **语义分轨（最高优先、改动最小、收益最大）**：`handleFindCompetitors` 回传从 `{items}` 改成 `{competitors:[app_store...], openSource:[github...]}`；前端分两区「🥊 商业竞品」/「🧰 相关开源」。不改关键字就显著变对。
  2. **关键字从身份证字段直接拼**：前端 `findCompetitors` 不再取「第一个非空整句」，改用 spec 字段拼**短关键字**（优先 `name` + `coreFeatures`/`targetUser` 取词，避免整句 oneLiner）；后端 `/find/competitors` 收 `keywords` 优先、`brief` 保底。**不另调模型**（驳斥结论：低频高容错不值得引 LLM；「教练顺手吐关键字」有时机耦合 bug——按钮触发与 proposal 时机对不上，弃用）。
  3. **查询调参+去重+防限流**：iTunes `country` 按关键字语言判（中文→tw、英文→us，别写死 tw）、`entity` 收窄；GitHub 加 `in:name,description`、`stars:>10`；按 url 去重；GITHUB_TOKEN 设进 fly secrets 防 10/min；部分来源失败回 partial 让前端可提示。
  4. **明确不做**：独立 Haiku 抽词、LLM 重排、黑名单/阈值表（过度工程）。

### 15.3 实作步骤（切 3 刀，每刀编译+测）
| 刀 | 内容 | 验收 |
| --- | --- | --- |
| 1 · 选项续聊 | applySpecPatch 回 Bool；AICoachView.tapProposal 成功才 append+send；proposalRow streaming 时锁钮；NoteChatViews 维持只记录 | 教练点选项→出现你的气泡→AI 问下一题；笔记点只记录 |
| 2 · 竞品分轨+关键字 | 后端回 {competitors,openSource}+收 keywords 优先+查询调参去重；前端 findCompetitors 拼短关键字、竞品分两组显示 | 商业竞品/开源分两区、相关度明显变好 |
| 3 · 验收 | E2E 不破；测全绿；bump build 10；archive 上传；GITHUB_TOKEN 设 fly secret | 全绿 + UPLOAD SUCCEEDED |

### 15.4 回归底线
- 单笔记聊天/编辑、守门员、主笔记锚点、四流不破；SSE 协议不动（竞品走既有 /find/competitors，只改回传形状+收 keywords）。

---

---

## 16. build 11 · 竞品/开源改用 Exa 神经搜寻（v6 · 2026-06-13；WebFetch 官方 API + Plan agent 定案，实测 key 通过）

> Exa key 已测(中文竞品+GitHub都准、$0.007/次、$10免费额度)。本节为 Plan agent 读真实代码+官方 API 字段后的定案。

### 16.1 Exa 能加值的功能清单（做/延后）
| 功能 | Exa 能力 | 决策 | 理由 |
|---|---|---|---|
| 竞品卡带「一句话简介」 | contents.summary.query | **build11** | 改动小、价值最高:一眼看懂每个竞品在做什么 |
| 开源轨用 category=github(neural) | category | **build11** | 换 Exa 核心,中文也准 |
| 「找更多类似」findSimilar | POST /findSimilar {url} | **build11(轻量入口)** | Exa 最大差异化卖点,实作量小 |
| AI 教练市场调查/痛点验证 | POST /answer(带引用) | 之后(build12) | 要接进对话流+引用渲染,跨度大会爆范围 |
| 开发笔记找参考资料 | /search web + contents.text | 之后 | 需新 UI 入口、与 find_info 重叠待梳理 |
| 市场趋势 news / 结构化竞品对比 outputSchema / 深度 /research | category=news / outputSchema / /research | 之后 | 锦上添花,先不做 |

**build11 锁定范围 = 竞品 neural+简介 + 开源 github + findSimilar 入口。其余明确推后。**

### 16.2 后端（server/src/index.js）
- 加 `EXA_API_KEY = process.env.EXA_API_KEY`；helper `exaSearch(query,{type,category,numResults,summaryQuery})` 打 `POST https://api.exa.ai/search`(header x-api-key)。
- `handleFindCompetitors` 改:
  - **竞品轨**:Exa web 搜 `query=kw+" 竞品/类似产品"`、type auto、numResults 8、`contents.summary.query="这产品一句话在做什么?繁中30字内"` → 每条带 summary;source 改 **`"web"`**(语义准:商业竞品/相关产品)。**砍 iTunes**(注释封存,额度耗尽可一行切回)。
  - **开源轨**:Exa `category:"github"`、numResults 6;**失败/空 → fallback 现有 GitHub 关键字搜**(免费保底)。
  - 去重(seen by url)、`AbortSignal.timeout(12000)`、两轨 `Promise.allSettled` 并行、任一失败 partial=true、EXA_API_KEY 缺失则竞品空+开源走 GitHub 保底(不崩)。
  - 回应保留 `{items,competitors,openSource,partial}`,每条多带 `summary`(向后相容,旧前端只读 items 仍可)。
- 新 `POST /find/similar {url}` → Exa findSimilar,复用 map。
- key 放 `fly secrets set EXA_API_KEY=...`(不进 git)。

### 16.3 前端
- `CompetitorItem` 加 `var summary: String?`(decodeIfPresent,旧资料安全)。
- `infoEnoughForCompetitors` 门槛**放低**:只要专案有名字(name/title 非空)→ 找竞品钮就出现(同步改 SystemSpecTests 断言)。
- `AICoachView` competitorChip 显示 `summary ?? subtitle`;分组键改 `app_store||web`→🥊商业竞品/相关产品、`github`→🧰相关开源;按钮文案改「相关产品+开源」。
- findSimilar:AIServicing/Live/Stub 加 `findSimilar(url:)`;结果区放一个轻量「找更多类似」入口。

### 16.4 切刀(每刀编译+测+可上 TestFlight)
| 刀 | 内容 | 验收 |
|---|---|---|
| 0 | `fly secrets set EXA_API_KEY`(无码) | secrets list 有 EXA_API_KEY |
| 1 | 后端竞品轨 Exa+summary、开源轨 Exa+GitHub保底、回应加 summary、/find/similar | curl 回 web竞品带简介+github开源;旧前端仍读 items(向后兼容) |
| 2 | 前端 CompetitorItem.summary+chip显示+分组键web/github+门槛放低+Stub/测同步 | build绿+测过+模拟器找竞品看到简介 |
| 3 | findSimilar 前端入口 | 点「找更多类似」回结果 |

### 16.5 回归底线
- 只动 find 这一流的内部 + 加 /find/similar；不碰 chat/optimize/structure。守门员(点 chip 才 addCompetitors 记入)不动。主笔记/身份证结构不动(只给 CompetitorItem 加可选 summary)。回应保留 items 向后相容。

---

> 文档结束（v6）。实作对照:§12~§16。build11 照 §16.4 切刀。
